## Introduction
This is an application build to interact with zerion api to save and load images
The application is built using angularjs and angular material themes for the ui

[Live Project](https://jolly-fermat-f37a18.netlify.com/#!/records/tile_view)

# Getting project ready
-----------------------

## Step 1: run *yarn install* unless you do not have yarn, in that case run *npm install
## Step 2: run *yarn start* unless you do not have yarn, in that case run *npm start*
## Step 3: open *http://localhost:8080* in a web browser

Thats all
